<% (1..12).each do |column| %>
  <div class="col-1 p-0">
    <% if rand < ((13.0 - column) / 12.0)%>
      <%= image_tag photos[((row * 12) + column) % photos.size].image, style: 'width: 100%;', class: "home_photo" %>
    <% end %>
  </div>
<% end %>